image: ruby:2.4.1

before_script:
  - gem install jekyll bundler
  - apt-get update -qq
  - curl -sL https://deb.nodesource.com/setup_7.x | -E bash -
  - apt-get install -y nodejs
  - npm install -g gulp
  - npm install

stages:
  - build

build:
  stage: build
  script: gulp
